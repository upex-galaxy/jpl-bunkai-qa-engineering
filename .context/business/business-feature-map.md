# Business Feature Map — placeholder

> **Run `/business-feature-map` (Claude Code command) to populate or refresh this file.**

A feature-centric complement to `business-data-map.md`: feature catalog with stable `FEAT-NNN` IDs, CRUD matrix, third-party integrations, feature flags, QA relevance tags per feature.

- **Soft input for**: `/master-test-plan`, `/business-api-map`.
- **Consumed by**: the `project-discovery` and `sprint-testing` skills for ATP planning and risk scoping.

The command replaces this placeholder with the full structure (feature catalog, CRUD matrix, integrations, feature flags, QA relevance matrix, discovery gaps). Keep this file checked in as the expected anchor.

See `.claude/commands/business-feature-map.md` for the exact output contract.
