# Project Configuration — Bunkai TMS

> Project: Bunkai (分解)
> Generated: 2026-06-19
> Source: Reverse-engineered from `c:/Projects/UPEX/upex-bunkai-tms/`

---

## Repositories

| Repository | Path | Branch | Purpose |
|------------|------|--------|---------|
| Target app (product under test) | `c:/Projects/UPEX/upex-bunkai-tms/` | main | Bunkai TMS — Next.js full-stack app |
| QA boilerplate | `c:/Projects/UPEX/jpl-bunkai-qa-engineering/` | main | KATA test framework (this repo) |

> Remote URL: Discovery Gap — `gh repo view` not run. See `.agents/project.yaml` for project key.

---

## Tech Stack

### Frontend
- Framework: Next.js 15 (App Router — `app/` directory)
- Language: TypeScript 5.9+
- Runtime: Bun 1.0+
- Styling: Tailwind CSS 3.4 + shadcn/ui (Radix primitives)
- State: React 19 server/client component model (no external state library)
- Tables: TanStack Table 8
- Notifications: Sonner
- Icons: Lucide React
- Command palette: cmdk
- Forms/validation: Zod 4

### Backend (BFF — Next.js API routes)
- Framework: Next.js API routes (`app/api/v1/`)
- Language: TypeScript (same repo — monolith)
- Auth: Supabase Auth (`@supabase/ssr`) — cookie-based session, magic link + email/password
- ORM: Supabase JS client (`@supabase/supabase-js`) — no separate ORM
- Validation: Zod schemas
- API spec: OpenAPI 3.x generated via `@asteasolutions/zod-to-openapi`, exposed at `/api/openapi`

### Database
- Type: PostgreSQL (via Supabase)
- Provider: Supabase (hosted)
- Auth: Supabase Auth (`auth.users`)
- RLS: Row-Level Security enabled on all public tables
- Migrations: `supabase/migrations/` (12 migrations, 0001–0012)
- Access: DBHub MCP (`dbhub.toml` present) + Supabase MCP (SUPABASE_ACCESS_TOKEN)

### Infrastructure
- Cloud: Vercel (inferred from `.env.example` Vercel+Supabase integration hint)
- CI/CD: **None** — no `.github/workflows/` found (Discovery Gap)
- Monitoring: None detected (Discovery Gap)

---

## Environments

| Environment | URL | Purpose | Access |
|-------------|-----|---------|--------|
| Local | `http://localhost:3000` | Development | Direct — `bun run dev` |
| Staging | Discovery Gap | Pre-prod testing | Discovery Gap |
| Production | Discovery Gap | Live product | Discovery Gap |

> Source: `lib/env.ts` — `NEXT_PUBLIC_APP_URL` defaults to `http://localhost:3000`.
> Staging/Production URLs not found in source — must be supplied by user or extracted from Vercel dashboard.

---

## Tools and Access

| Tool | Value | Source |
|------|-------|--------|
| Issue tracker | Jira | `.agents/project.yaml` |
| Atlassian URL | `https://upexgalaxy69.atlassian.net/` | `.agents/project.yaml` |
| Project key | BK | `.agents/project.yaml` |
| DB MCP | DBHub (`dbhub.toml`) + Supabase MCP | `dbhub.toml` + `.env.example` |
| API explorer | `/api/docs` (Scalar UI) + `/api/openapi` (JSON spec) | `app/api/docs/page.tsx`, `app/api/openapi/route.ts` |
| Docs | DESIGN.md, CONTEXT.md, `.context/designs/` | In-repo |

---

## API Contract Source

OpenAPI spec exposed at runtime: `GET /api/openapi` (JSON).
Technical TypeScript types generated by: `bun run api:sync` → `api/openapi-types.ts`.
Business angle: `/business-api-map` command (post-discovery).

---

## Access Checklist

- [x] Repository read access — local clone confirmed
- [x] Supabase project connected — env vars in `.env` (ATLASSIAN_* confirmed)
- [ ] Database MCP access — DBHub config present but connectivity unverified
- [x] Issue tracker access — ATLASSIAN_URL + ATLASSIAN_EMAIL + ATLASSIAN_API_TOKEN in `.env`
- [ ] Staging environment reachable — URL unknown (Discovery Gap)
- [ ] CI/CD visibility — no CI pipeline found

---

## Discovery Gaps

- [ ] **Staging/Production URLs**: Not in source. Check Vercel dashboard or ask team.
- [ ] **Supabase project ref**: Needed for DBHub host pattern (`db.<ref>.supabase.co`). Check `NEXT_PUBLIC_SUPABASE_URL` value in `.env`.
- [ ] **CI/CD pipeline**: No `.github/workflows/` found. Verify if deploys are Vercel-only or if a pipeline exists elsewhere.
- [ ] **Monitoring**: No Sentry, Datadog, or equivalent detected. Verify with team.
- [ ] **Env var inconsistency**: `lib/env.ts` uses `NEXT_PUBLIC_SUPABASE_ANON_KEY` but `.env.example` declares `SUPABASE_PUBLISHABLE_KEY`. Confirm which is canonical (may be a naming refactor in progress).
- [ ] **Remote repo URL**: Not retrieved. Run `gh repo view` to get the GitHub remote.
- [ ] **Pricing / plan limits**: `WorkspacePlan` enum has `community | cloud | enterprise` but limits/features per plan are not in source.
